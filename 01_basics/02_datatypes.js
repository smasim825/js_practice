"use strict"; //treat all JS as newer version

// alert(3+3) we are using nodejs not brower (giving alert popup in brower)

let name = "asim"  
let age = 18 
// string type => " "
// number => type (range 2^53)
// bigint => if number it too much bit
// boolean => true/false
// undefined => null/ nullptr which value is not assigned
// symbol => to find uniqness , use broadly in react

// object 


console.log(typeof name) //retrun variable type

//null is an object

// x ** y it means => x^y
// x>>>y unsigned right shift
// x === y | x !== y =>used for BigInt equal